#!/usr/bin/env bun

import pkg from '@oclif/core'
const { run, flush, Errors, settings } = pkg

// In dev mode, always show stack traces
process.env.NODE_ENV = 'development'
settings.debug = true

// Start the CLI
run(process.argv.slice(2), import.meta.url).then(flush).catch(Errors.handle)
