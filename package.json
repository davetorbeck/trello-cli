{
  "name": "trello-cli-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "bun run --filter '*' test",
    "dev": "bun run --filter '*' --parallel dev",
    "build": "bun run --filter '@trello-cli/config' build && bun run --filter '@trello-cli/cache' build && bun run --filter 'trello-cli' build",
    "clean": "rm -rf packages/*/dist packages/*/*.tsbuildinfo"
  }
}
